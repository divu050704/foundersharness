export default function DedicatedOfficeLayout({ children }) {
  return (
    <div className="h-screen w-screen overflow-hidden bg-[#0c0f12] text-[#f3f4f6] font-mono">
      {children}
    </div>
  );
}
